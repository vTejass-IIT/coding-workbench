// LC 212 — Word Search II.
// Given an m x n board of letters and a list of words, return every word from the
// list that can be formed by a path of adjacent (4-directional, non-reused) cells.
export function findWords(board: string[][], words: string[]): string[] {
  throw new Error("not implemented");
}
