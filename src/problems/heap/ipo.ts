// LC 502 — IPO.
// You start with `w` capital and may complete up to `k` projects (each completed at
// most once). profits[i]/capital[i] describe project i; you can only start a project
// whose capital requirement is <= your current capital. Return the maximum capital
// achievable after choosing at most k projects greedily.
// Input: k = 2, w = 0, profits = [1,2,3], capital = [0,1,1] -> Output: 4
export function findMaximizedCapital(k: number, w: number, profits: number[], capital: number[]): number {
  throw new Error("not implemented");
}
