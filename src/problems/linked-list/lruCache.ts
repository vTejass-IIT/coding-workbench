// LC 146 — LRU Cache.
// Design a fixed-capacity cache with get/put in O(1) average time. get(key) returns
// the value or -1 and marks the key as recently used; put(key, value) inserts/updates
// and evicts the least-recently-used entry when over capacity.
export class LRUCache {
  constructor(capacity: number) {
    throw new Error("not implemented");
  }

  get(key: number): number {
    throw new Error("not implemented");
  }

  put(key: number, value: number): void {
    throw new Error("not implemented");
  }
}
