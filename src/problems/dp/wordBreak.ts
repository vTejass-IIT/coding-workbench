// LC 139 — Word Break.
// Return whether s can be segmented into a space-separated sequence of one or more
// dictionary words from wordDict (words may be reused any number of times).
// Input: s = "leetcode", wordDict = ["leet","code"] -> Output: true
export function wordBreak(s: string, wordDict: string[]): boolean {
  throw new Error("not implemented");
}
