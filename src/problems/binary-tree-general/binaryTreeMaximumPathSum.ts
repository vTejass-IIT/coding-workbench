// LC 124 — Binary Tree Maximum Path Sum.
// A path is any non-empty sequence of nodes connected by parent-child edges, and
// need not pass through the root. Return the maximum path sum over all paths.
// Input: root = [-10,9,20,null,null,15,7] -> Output: 42 (15 -> 20 -> 7)
import { TreeNode } from "./treeNode";

export function maxPathSum(root: TreeNode | null): number {
  throw new Error("not implemented");
}
