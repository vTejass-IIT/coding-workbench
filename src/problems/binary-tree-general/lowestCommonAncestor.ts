// LC 236 — Lowest Common Ancestor of a Binary Tree.
// Return the lowest node that has both p and q as descendants (a node can be a
// descendant of itself). p and q are guaranteed to exist in the tree.
import { TreeNode } from "./treeNode";

export function lowestCommonAncestor(root: TreeNode | null, p: TreeNode, q: TreeNode): TreeNode | null {
  throw new Error("not implemented");
}
