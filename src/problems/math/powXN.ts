// LC 50 — Pow(x, n).
// Implement pow(x, n), i.e. x^n, for a double base and integer exponent (which may be
// negative), in O(log n) time via fast exponentiation.
// Input: x = 2.0, n = 10 -> Output: 1024.0
export function myPow(x: number, n: number): number {
  throw new Error("not implemented");
}
