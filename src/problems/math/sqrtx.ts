// LC 69 — Sqrt(x).
// Return floor(sqrt(x)) for a non-negative integer x, without using a built-in
// power/sqrt function.
// Input: x = 8 -> Output: 2
export function mySqrt(x: number): number {
  throw new Error("not implemented");
}
