// LC 98 — Validate Binary Search Tree.
// Return whether the tree satisfies the BST property: every node's left subtree
// contains only values strictly less than it, and its right subtree only values
// strictly greater (checked transitively, not just against the immediate parent).
import { TreeNode } from "../binary-tree-general/treeNode";

export function isValidBST(root: TreeNode | null): boolean {
  throw new Error("not implemented");
}
