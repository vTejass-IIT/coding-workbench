// LC 130 — Surrounded Regions.
// Flip every 'O' to 'X' in place, except 'O's connected (4-directionally) to a
// border 'O', which must stay 'O' (they aren't "surrounded").
export function solve(board: string[][]): void {
  throw new Error("not implemented");
}
