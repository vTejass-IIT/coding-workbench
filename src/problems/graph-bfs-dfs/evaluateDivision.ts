// LC 399 — Evaluate Division.
// equations[i] = [Ai, Bi] with values[i] meaning Ai / Bi = values[i]. For each query
// [Cj, Dj], return Cj / Dj if derivable via the given equations (treat them as a
// weighted graph), else -1.0.
// Input: equations = [["a","b"],["b","c"]], values = [2.0,3.0], queries = [["a","c"]]
// Output: [6.0]
export function calcEquation(equations: string[][], values: number[], queries: string[][]): number[] {
  throw new Error("not implemented");
}
