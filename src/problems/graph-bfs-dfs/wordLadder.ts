// LC 127 — Word Ladder.
// Each step changes exactly one letter, and every intermediate word must be in
// wordList. Return the number of words in the shortest transformation sequence from
// beginWord to endWord (counting both), or 0 if none exists.
// Input: beginWord = "hit", endWord = "cog", wordList = ["hot","dot","dog","lot","log","cog"]
// Output: 5 (hit -> hot -> dot -> dog -> cog)
export function ladderLength(beginWord: string, endWord: string, wordList: string[]): number {
  throw new Error("not implemented");
}
