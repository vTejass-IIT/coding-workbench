// LC 210 — Course Schedule II.
// Same setup as Course Schedule, but return a valid course order that satisfies all
// prerequisites, or [] if no valid order exists (a cycle).
// Input: numCourses = 4, prerequisites = [[1,0],[2,0],[3,1],[3,2]] -> Output: [0,1,2,3] (one valid order)
export function findOrder(numCourses: number, prerequisites: number[][]): number[] {
  throw new Error("not implemented");
}
