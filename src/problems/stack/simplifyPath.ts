// LC 71 — Simplify Path.
// Convert a Unix-style absolute path to its canonical form: collapse "." (no-op),
// resolve ".." (pop the previous directory), collapse repeated slashes, and drop the
// trailing slash unless the result is the root.
// Input: path = "/a/./b/../../c/" -> Output: "/c"
export function simplifyPath(path: string): string {
  throw new Error("not implemented");
}
