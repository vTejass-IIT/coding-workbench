// LC 134 — Gas Station.
// gas[i] is fuel available at station i; cost[i] is fuel needed to travel from
// station i to station i+1. Return the starting station index that allows a full
// circuit, or -1 if impossible (the answer is guaranteed unique if it exists).
// Input: gas = [1,2,3,4,5], cost = [3,4,5,1,2] -> Output: 3
export function canCompleteCircuit(gas: number[], cost: number[]): number {
  throw new Error("not implemented");
}
