// LC 27 — Remove Element.
// Remove all occurrences of `val` from nums in place; return the new length k.
// The first k elements must hold the result (order doesn't matter); anything
// beyond index k is ignored by the judge.
// Input: nums = [3,2,2,3], val = 3 -> Output: 2, nums[0..1] = [2,2] (any order)
export function removeElement(nums: number[], val: number): number {
  throw new Error("not implemented");
}
