// LC 135 — Candy.
// Each child gets at least 1 candy; any child with a higher rating than an adjacent
// neighbor must receive more candy than that neighbor. Return the minimum total candies.
// Input: ratings = [1,0,2] -> Output: 5 (distribution [2,1,2])
export function candy(ratings: number[]): number {
  throw new Error("not implemented");
}
