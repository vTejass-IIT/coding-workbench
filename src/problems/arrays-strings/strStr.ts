// LC 28 — Find the Index of the First Occurrence in a String (strStr).
// Input: haystack = "sadbutsad", needle = "sad" -> Output: 0
export function strStr(haystack: string, needle: string): number {
  throw new Error("not implemented");
}
