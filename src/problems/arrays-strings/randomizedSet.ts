// LC 380 — Insert Delete GetRandom O(1).
// Design a structure supporting insert/remove/getRandom, each averaging O(1) time.
// insert(val): add val if absent, return whether it was added.
// remove(val): remove val if present, return whether it was removed.
// getRandom(): return a uniformly random element currently in the set.
export class RandomizedSet {
  constructor() {
    throw new Error("not implemented");
  }

  insert(val: number): boolean {
    throw new Error("not implemented");
  }

  remove(val: number): boolean {
    throw new Error("not implemented");
  }

  getRandom(): number {
    throw new Error("not implemented");
  }
}
