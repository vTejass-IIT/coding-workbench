// LC 68 — Text Justification.
// Pack words greedily into lines of exactly maxWidth characters. For fully packed
// lines, distribute spaces as evenly as possible, with extra space going to the
// leftmost gaps first. The last line (and any line with only one word) is
// left-justified with single spaces and padded with trailing spaces.
export function fullJustify(words: string[], maxWidth: number): string[] {
  throw new Error("not implemented");
}
