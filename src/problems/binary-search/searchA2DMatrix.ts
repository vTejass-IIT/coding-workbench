// LC 74 — Search a 2D Matrix.
// Each row is sorted ascending, and each row's first element is greater than the
// previous row's last element (i.e. the matrix is sorted as if flattened). Return
// whether target exists, in O(log(m*n)) time.
export function searchMatrix(matrix: number[][], target: number): boolean {
  throw new Error("not implemented");
}
