// LC 290 — Word Pattern.
// Return whether s follows the same bijective pattern as `pattern`, where each
// pattern letter maps to exactly one space-separated word in s and vice versa.
// Input: pattern = "abba", s = "dog cat cat dog" -> Output: true
export function wordPattern(pattern: string, s: string): boolean {
  throw new Error("not implemented");
}
