// LC 289 — Game of Life.
// Update the board in place to its next state under Conway's rules, using the
// 8-neighbor rule: a live cell with 2-3 live neighbors survives; a dead cell with
// exactly 3 live neighbors becomes alive; all other cells die/stay dead.
export function gameOfLife(board: number[][]): void {
  throw new Error("not implemented");
}
