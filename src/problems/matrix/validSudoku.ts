// LC 36 — Valid Sudoku.
// Determine whether a 9x9 board is valid: each row, column, and 3x3 sub-box must
// contain the digits 1-9 without repetition. '.' denotes an empty cell and is ignored;
// the board does not need to be solvable, only currently consistent.
export function isValidSudoku(board: string[][]): boolean {
  throw new Error("not implemented");
}
